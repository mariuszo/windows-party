<UserControl x:Class="Tesonet.Windows.Party.Views.ServerListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <StackPanel>
            <StackPanel Orientation="Horizontal">
                <Label Content="testio."/>
                <Button Content="Log out" Command="{Binding LogOutCommand}"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal">
                <DataGrid ItemsSource="{Binding Servers}"
                          AutoGenerateColumns="False"
                          CanUserAddRows="False">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="SERVER" Binding="{Binding Name}" />
                        <DataGridTextColumn Header="DISTANCE" Binding="{Binding Distance}" />
                    </DataGrid.Columns>
                </DataGrid>
                <Image />
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>
